export interface IUser {
    _id: string
    firstName: string
    lastName: string
    email: string
    password: string
    terms: boolean
    createdAt: string
    updatedAt: string
    lastLogin: string
    active: boolean
}
